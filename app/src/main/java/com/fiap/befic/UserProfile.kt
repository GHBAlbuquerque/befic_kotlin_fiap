package com.fiap.befic

import android.os.Bundle
import android.view.View
import android.widget.BaseAdapter
import androidx.appcompat.app.AppCompatActivity


class UserProfile : AppCompatActivity() {
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContentView(R.layout.activity_user_profile)

        /*val stories = ArrayList<String>()
        stories.addAll(listOf<String>("Paixão Avassaladora", "Meu Vizinho Mafioso", "When I Kissed The Teacher," +
                "Até o Outro Luar", "Coisas extraordinárias"))

        val storyList = findViewById<View>(R.id.storyList)
        val adapter = BaseAdapter(this, stories)
        stories.setAdapter(customAdapter)*/


    }
}